# ==========================================
# .env.example — copy to .env.local for local dev
# (never commit .env.local to git)
# ==========================================

# ── Supabase ──────────────────────────────
# Find these in: Supabase Dashboard → Project Settings → API
VITE_SUPABASE_URL=https://dngoxhunrqadsfubqvrh.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImRuZ294aHVucnFhZHNmdWJxdnJoIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzM0MjMwNTQsImV4cCI6MjA4ODk5OTA1NH0.gWhOqOhUEQ3OKyLZBeRCHGa-YXoBRZEKQdrQv9lcSKs

# ── Claude / Anthropic ────────────────────
# Find this in: https://console.anthropic.com → API Keys
# Used only in the Vercel serverless function (api/generate-sentence.js)
# NOT prefixed with VITE_ — it stays server-side only
ANTHROPIC_API_KEY=sk-ant-api03-...

# ── Firebase (existing) ───────────────────
# These are already in your firebase config — no changes needed
# They are embedded in src/utils/firebase.js via import.meta.env
# VITE_FIREBASE_API_KEY=...
# VITE_FIREBASE_AUTH_DOMAIN=...
# VITE_FIREBASE_PROJECT_ID=...
